#include <stdio.h>
#include <string.h>

void preprocessing (char *navn_paa_fil) 
{
    int text_size=10; /*Ændre størrelsen!!*/
    char text[text_size]; 
    strlen(text);

    for (int i = 0; i<=text_size; i++) 
    {
        text[i];

    }
}